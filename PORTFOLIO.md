# 빨래빨래 (Ppallae Ppallae)

**날씨·위치 기반으로 "지금 빨래를 널면 잘 마르는지" 알려주는 모바일 앱**
Android / Web (iOS 예정) · 기획부터 앱·백엔드·인프라까지 1인 개발

> 기간: `2025.04 ~ 진행 중` *(채우기)* · 역할: 전 영역 단독 개발 · 형태: 개인 프로젝트
> 데모: `스토어/웹 링크` *(채우기)* · 코드: `GitHub 링크` *(채우기)*

---

## 1. 프로젝트 개요

### 문제 정의
일기예보는 "오늘 비 온다"까지만 말해준다. 정작 빨래하는 사람이 궁금한 건 따로 있다 —
**"지금 널면 마르나? 못 마르면 몇 시에 널어야 하나? 얼마나 걸리나?"**
기온·습도·바람·미세먼지를 각각 보여주는 날씨 앱은 많지만, 이걸 **하나의 의사결정**으로 환산해주는 서비스는 없었다.

### 해결 방향
원본 기상 수치를 나열하는 대신, 증발에 영향을 주는 변수(기온·습도·바람·하늘상태·강수·미세먼지)를
**VPD(포화수증기압차) 기반 건조력 모델**로 환산해 세 가지 답을 준다:

1. **빨래지수 0~100점 + 등급** (EXCELLENT ~ BAD)
2. **예상 건조시간** (빨래 두께 × 건조 장소별 차등)
3. **추천 시작 시간** (시간별 예보에 누적 건조력을 적용해 역산)

### 주요 사용자
- 베란다·실외 건조를 하는 1인 가구 / 주부 — "지금 vs 이따"를 매일 결정하는 사람
- 앱을 열지 않고도 판단하고 싶은 사용자 → **Android 홈 위젯**으로 점수·추천시간 상시 노출

### MVP 범위
- ✅ GPS 기반 현재 지역 빨래지수 + 시간별/일별 예보 + 추천 시작 시간
- ✅ 빨래 종류 3종(얇음/중간/두꺼움) × 건조 장소(실외/베란다/실내)별 차등 계산
- ✅ 카카오맵 지역 검색·탭 조회, 내 주변 빨래방(카카오 로컬)
- ✅ Android 홈 위젯 (1x1 / 3x1)
- ✅ 관리자 웹(공지·FAQ·앱설정·기능 플래그 운영)
- ⏸ iOS 출시, 푸시 알림, Firebase Crashlytics/Analytics는 베타 이후

> 초기엔 빨래 종류를 16종으로 세분화했다가 **"사용자는 수건과 청바지를 구분해서 널지 않는다"**는 판단으로
> 3종(두께 기준)으로 단순화 — 정확도보다 입력 부담을 줄이는 쪽이 실사용에 유리하다고 결정.

---

## 2. 핵심 기능 요약

| 기능 | 설명 | 기술 포인트 |
|---|---|---|
| **빨래지수 산출** | 기상청 실황+예보를 0~100점·등급·건조시간으로 환산 | 시간당 건조력 = `10 × VPD × 바람·일사·장소·강수 계수`. 순수 함수로 분리해 단위 테스트 커버 |
| **추천 시작 시간** | "몇 시에 널어야 오늘 안에 마르나"를 역산 | 시간별 예보에 누적 건조력을 적용, 목표 건조량(두께별 35/65/110) 도달 시각 탐색 |
| **현재 위치 자동 인식** | GPS → 좌표 → 행정동 변환 후 즉시 조회 | 첫 fix 오차 100m 초과 시 재샘플링. 실패 시 기본 지역 폴백 + 화면에 폴백 배너 명시 |
| **지역 검색 / 지도** | 카카오맵에서 탭·검색으로 타 지역 조회 | 웹/모바일을 조건부 임포트(`_web`/`_mobile`)로 분기, 단일 코드베이스 유지 |
| **내 주변 빨래방** | 카카오 로컬 검색 결과 표시 | 응답에 `source` 필드 포함 — mock 폴백 시 화면에 배지로 구분 표시 |
| **Android 홈 위젯** | 등급별 배경색 + 점수 + 추천시간 상시 노출 | Flutter → SharedPreferences → Kotlin `RemoteViews` Provider 2종(1x1/3x1) |
| **표준 에러 코드** | 모든 실패를 `LOC-/API-/WGT-` 코드로 분류해 사용자 메시지에 노출 | 비개발자의 스크린샷 제보만으로 실패 단계 즉시 식별 (예: `[API-002] 응답 시간 초과`) |
| **관리자 콘솔** | 공지·FAQ·문의·앱설정·기능 플래그 운영 | JWT 인증, 감사 로그, 외부 API 사용량 대시보드 |

### 관리자 웹 (Next.js 운영 콘솔) — 별도 저장소

앱과 같은 NestJS API의 `admin` 라우트(JWT 보호)를 쓰는 **운영 전용 웹 콘솔**. 12개 탭으로 구성:

| 영역 | 탭 | 하는 일 |
|---|---|---|
| **모니터링** | 대시보드 · 데이터 소스 · API 사용량 | 지역/공지/문의 현황 카드, **날씨 수집 커버리지(신선 지역 n/전체 m)**, 기상청·에어코리아·카카오 헬스 상태, 외부 API 일별 호출량 (쿼터 관리) |
| **콘텐츠 운영** | 공지 · FAQ · 문의 · 약관 버전 | CRUD + 활성/노출 제어. 약관은 버전 관리로 개정 이력 유지 |
| **서비스 설정** | 빨래 종류 · 앱 설정 · 기능 플래그 | 기준 건조시간·설명·주의문구, 약관 URL 등 앱 설정, 기능 on/off — **전부 무배포로 앱에 반영** |
| **거버넌스** | 감사 로그 · 보안 | 관리자 변경 이력 추적, 운영 전 보안 체크리스트(쿠키 전환·Swagger 비공개·도메인 보호) 명문화 |

> *(로그인 화면 + 대시보드 스크린샷 1~2장 자리)*

**설계 포인트** — "운영 중 코드 배포 없이 바꿀 수 있어야 하는 것"(콘텐츠·설정·플래그)을 전부 DB+관리자 웹으로 밀어냈고,
모든 관리자 변경은 감사 로그에 남는다. 관리자 시크릿은 앱·문서 어디에도 존재하지 않는다.

---

## 3. 시스템 구조 · 기술 스택

```
[Flutter 앱 / Android 위젯]
      │  HTTPS (base URL만 --dart-define 주입)
      ▼
[NestJS API] ─── Prisma ──> PostgreSQL(PostGIS)
      │      └── ioredis ──> Redis (캐시 · BullMQ 큐)
      ├──> 기상청 KMA API (초단기실황 + 단기예보)
      ├──> 에어코리아 API (미세먼지)
      └──> 카카오 REST API (역지오코딩 · 로컬 검색)

[Next.js 관리자 웹] ──> 동일 NestJS API (admin 라우트, JWT)
```

**데이터 파이프라인** — 요청 시 Redis 캐시 조회 → 미스면 온디맨드 수집(KMA/에어코리아) → 캐시+DB 저장.
외부 API 장애 시 DB의 마지막 스냅샷으로 폴백하되 응답에 `stale: true`를 명시 (90분 초과 데이터도 stale 처리).
모든 응답에 `source` 배열을 포함해 **데이터 출처를 클라이언트까지 추적 가능**하게 설계.

**저장소 구성** — 책임별 6개 저장소 분리: 앱 / API / 관리자 웹 / 고객센터 / 정책문서 / 인프라(Docker).

**보안 원칙** — 앱에는 API base URL만 빌드타임 주입. 기상청·카카오 REST·JWT·DB 자격 등
모든 비밀값은 백엔드 `.env`에만 존재 (카카오맵 JS 키만 예외적으로 클라이언트 사용).

**스택**: Flutter(Dart) · NestJS(TypeScript) · Prisma · PostgreSQL+PostGIS · Redis · BullMQ · Next.js · Docker · Kotlin(위젯)

---

## 4. 트러블슈팅 · 기술적 문제 · 한계와 개선 예정

### ① 외부 API 의존 서비스의 가용성 설계
기상청·에어코리아·카카오 중 하나만 죽어도 앱이 빈 화면이 되는 구조를 피해야 했다.
- **키 유무로 mock↔실데이터 자동 전환**되는 프로바이더 팩토리 — 키 없는 로컬 환경에서도 전체 플로우 개발 가능
- 실 API 실패 시 **DB 스냅샷 폴백 + `stale` 플래그** — 데이터가 없는 것보다 "오래된 데이터임을 알리고 보여주는" 쪽 선택
- 응답 `source` 배열로 mock/캐시/실데이터를 클라이언트까지 전파 → 화면에서 mock 배지 표시

### ② 실기기 테스트에서 만난 네트워크 3중 장벽
에뮬레이터·웹에서 잘 되던 앱이 실기기에서 전부 타임아웃. 원인이 세 겹이었다:
1. **localhost의 함정** — 폰에서 `localhost`는 폰 자신. PC LAN IP를 `--dart-define`으로 주입하도록 설계돼 있었지만, PC에 이더넷·Wi-Fi 서브넷이 달라 **폰이 붙은 네트워크의 IP**를 골라야 했다 (adb로 폰의 `wlan0` 주소를 직접 확인해 검증)
2. **Windows 방화벽 + 네트워크 프로필** — 4000 포트 인바운드 규칙이 없었고, Wi-Fi가 "공용" 프로필이라 Private 규칙이 무효 → 프로필 변경 + 규칙 추가
3. **Android cleartext 차단** — 평문 HTTP 기본 차단이라 `usesCleartextTraffic`(개발용) 허용, 운영은 HTTPS 전환 후 제거 예정

### ③ Docker 포트가 "이유 없이" 안 열리는 문제 — Windows 동적 포트 예약
어느 날부터 PostgreSQL 컨테이너(호스트 5433)가 healthy인데도 접속 불가. `docker port`는 매핑이 있다고 하는데 호스트에 리스너가 없었다.
- 진단: `netsh interface ipv4 show excludedportrange` → **재부팅 시 Hyper-V/WinNAT가 5358–5457 범위를 선점**, 5433이 포함됨
- Redis(16379)는 범위 밖이라 정상 동작 → "DB만 안 되는" 혼란스러운 증상의 원인
- 해결: `winnat` 서비스 재시작으로 동적 예약 해제 후 컨테이너 재기동. 재발 가능하므로 진단·해결 절차를 문서화

### ④ 디버그 로그가 거짓말을 한 사례 — Dart 생성자 파라미터 섀도잉
실기기 로그에 `[PpallaeApi] base=null`이 찍혀 "URL 주입 실패"로 오인할 뻔했다.
실제로는 생성자 본문에서 같은 이름의 **nullable 파라미터가 필드를 가려서**(shadowing) null이 출력된 것 —
필드는 정상 주입돼 있었다. `this.baseUrl` 명시로 수정. *"로그도 코드다, 로그 자체를 의심하라"*는 교훈.

### ⑤ 기술 선택: FastAPI → NestJS 교체
초기 백엔드는 FastAPI였으나, 관리자 웹(Next.js)과의 타입 공유·DI 기반 모듈 구조·BullMQ 생태계를 고려해
**TypeScript로 통일(NestJS)**하기로 결정. 프론트~백 단일 언어로 컨텍스트 스위칭 비용을 줄이고,
프로바이더 팩토리(mock 전환) 같은 DI 패턴을 자연스럽게 활용.

### 한계와 개선 예정
| 항목 | 현재 상태 | 계획 |
|---|---|---|
| iOS | 미출시 (빌드에 Mac 필수) | Android 베타 안정화 후 클라우드 Mac/CI로 빌드·TestFlight |
| 통신 보안 | 개발 중 HTTP(cleartext 허용) | 운영 도메인 + HTTPS 전환, cleartext 제거 |
| 카카오 키 | 테스트 JS 키 | 출시 전 비즈니스 키 재등록 |
| 관측성 | 표준 에러 코드(클라이언트) | Firebase Crashlytics + Analytics 연동 |
| 관리자 인증 | localStorage 토큰 (개발용) | httpOnly secure cookie 전환, 관리자 도메인 접근 제한 |
| 테스트 | 점수 계산기·프로바이더 단위 테스트, 앱 스모크 테스트 | 통합 테스트·위젯 테스트 커버리지 확대 |
| 알림 | 없음 | "오늘 빨래 골든타임" 푸시 알림 |

---

## 5. 빨래 점수 산정 기준 · 정책 · 데이터 출처

### 5.1 점수 산정 기준 (V2 — 시간대별 누적 건조력 모델)

**핵심 아이디어**: 빨래는 "지금 이 순간의 날씨"가 아니라 **널고 나서 몇 시간 동안의 날씨 누적**으로 마른다.
그래서 단일 시점 점수가 아니라, 시간대별 예보에 시간당 건조력을 적용해 누적한다.

```
시간당 건조력 = 10 × VPD × 바람계수 × 햇빛계수 × 장소계수 × 강수계수
```

| 인자 | 계산 | 근거 |
|---|---|---|
| **VPD** (포화수증기압차) | `0.6108·e^(17.27T/(T+237.3)) × (1 − RH/100)` kPa, floor 0.05 | Magnus(Tetens) 식. 증발의 물리적 추진력 |
| **바람계수** | `1 + 0.25·√풍속`, 1.0~1.8 클램프 | 포화된 경계층 공기 제거 효과 (체감 수확 체감) |
| **햇빛계수** | 낮: 맑음 1.35 / 구름 1.15 / 흐림 1.0 · 밤: 0.85 | 증발 잠열 공급. 낮 기준 07~18시(KST) |
| **장소계수** | 실외 1.0 / 베란다 0.65 / 실내 0.38 | 통풍·일사 접근성 차등 |
| **강수계수** | 실제 강수 시 실외 0.05 / 베란다 0.45 / 실내 0.9 · POP≥70%면 실외 0.3 | 실제 강수(PTY·강수량) > 강수확률(POP) 우선순위 |

**점수·등급·추천시간 산출**

- 빨래별 **목표 건조량**: 얇음 35 / 중간 65 / 두꺼움 110 (+ 빨래 양에 따라 배수 적용)
- `점수 = min(100, 누적 건조력 ÷ 목표 건조량 × 100)` → 여기에 리스크 감점(건조 완료 전 강수, 야간 미건조, 영하+실외, 강풍 등)을 반영한 `adjustedScore`가 사용자에게 표시됨
- **등급 임계값**: 85 EXCELLENT / 70 GOOD / 50 NORMAL / 30 BAD — 서버·Flutter 양쪽에 동일 상수로 두어 UI 불일치 방지
- **추천시간 = 세탁기를 돌리기 시작하는 시각** (너는 시각이 아님). 지금부터 30시간을 1시간 간격 후보로 생성, `널기 시작 = 후보 + 세탁 45분` 이후 최대 16시간 건조 창을 평가. 최고 품질과 근소차(±10)면 상식적 시간대(07~22시)의 **가장 이른** 시작을 추천 (새벽 추천 방지), 목표 도달 순간을 보간해 **건조 완료 시각**까지 산출
- V1(단일 시점 가감점 모델: 기준 100점에서 기온·습도·바람·강수·하늘·미세먼지 감점)은 **hourly 예보가 없을 때의 폴백**으로 유지

모든 계산 함수는 순수 함수로 분리해 단위 테스트로 검증 (`dry-power.spec.ts`, `recommend.spec.ts`, `score-calculator.spec.ts`).

### 5.2 설정 · 운영 정책

| 정책 | 내용 |
|---|---|
| **데이터 신선도** | Redis 캐시 우선 → 미스 시 온디맨드 수집 → 실패 시 DB 마지막 스냅샷 폴백. 관측 90분 초과 데이터는 `stale: true`로 명시 후 제공 |
| **결측치 처리** | 시간대별 예보 결측 시 직전 시간 값 → 그것도 없으면 보수적 기본값(기온 20°C, 습도 70%, 바람 0.5m/s, 흐림) |
| **mock 전환** | 외부 API 키 부재 또는 강제 플래그 시 mock 프로바이더 자동 선택. 응답 `source`에 명시되고 앱 화면에도 배지 표시 |
| **운영 가변 설정** | 빨래 종류(기준 건조시간)·공지·FAQ·약관 URL·기능 플래그는 DB에 두고 관리자 웹에서 무배포 변경 |
| **비밀값 정책** | 앱에는 API base URL만 빌드타임 주입. 외부 API 키·JWT·DB 자격은 백엔드 `.env` 전용 (`.env.example`만 버전관리) |

### 5.3 데이터 출처 및 저작권

| 데이터 | 출처 | 이용 조건 |
|---|---|---|
| 기상 실황·예보 | **기상청** 초단기·단기(동네)예보 API | 공공데이터 — 공공누리 출처표시 준수, "기상청 자료 기반 가공" 고지 |
| 미세먼지 | **한국환경공단 에어코리아** 대기오염정보 API | 공공데이터 — 실시간 자료는 미검증 자료임을 고지 필요 |
| 지도·장소 검색 | **카카오맵 / 카카오 로컬 API** | Kakao Developers 약관 — 지도 저작권(© Kakao) 표기 유지, 검색 결과 저작권은 카카오 및 콘텐츠 제공자 |

> **고지 정책**: 빨래지수·예상 건조시간·추천 시작 시간은 위 원본 데이터를 자체 알고리즘으로 **가공한 참고용 정보**이며,
> 원본 수치가 아님을 앱 내에 명시한다. 원본 데이터의 저작권은 각 제공 기관에 있다.
